# Execute

Use the task and runtime context to update `{target}`.

Emit one final route tag:
- `<route>done</route>` when the work is complete
- `<route>retry</route>` when another loop is needed
- `<route>blocked</route>` when you cannot continue safely
