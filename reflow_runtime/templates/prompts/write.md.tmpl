# Write

Use the task and runtime context to update `{target}`.

When you are finished, write any necessary feedback to `review_notes.md` and emit one final route tag:
- `<route>done</route>` when the draft is ready for review
- `<route>blocked</route>` when you cannot continue safely
- `<route>revise</route>` only if you need another self-pass before review
