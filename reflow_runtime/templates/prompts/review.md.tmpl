# Review

Review `{target}` against the task and declared outputs.

Write actionable feedback to `review_notes.md`, then emit one final route tag:
- `<route>done</route>` if the workflow can complete
- `<route>revise</route>` if the writer needs another pass
- `<route>blocked</route>` if the task cannot proceed safely
