## Doc-Loop Progress Log

2026-03-13 Writer: Expanded `SAD.md` into a canonical implementation contract by defining shared path/encoding rules, `active.json`, config validation, run/history/meta schemas, shell-step artifacts, provider env/timeout behavior, run start/terminalization/resume/stop semantics, and minimal CLI output guarantees. Consolidated the failure model so provider process failures are the canonical terminal `step_failed` cases, while route-tag and required-file retry exhaustion stays defined once under `max_loops_exceeded` instead of being restated inconsistently in multiple sections.
2026-03-13 Verifier: `SAD.md` is close, but it is not implementation-ready yet. Fix these specific issues in the document itself:

1. Define the crash/recovery persistence point for step-loop increments and iteration numbering. Right now Section 12 says loop counts increment before launch and interrupted iterations count, but Section 10.4 only requires `run.json` writes after completed iterations and terminal transitions. A conforming implementation could crash after creating `steps/<step>/003/` but before persisting `step_loops[step] = 3`, and `resume` would then either undercount loops or reuse the same iteration number. Add one canonical rule for when `run.json` is rewritten relative to incrementing the loop counter and creating the iteration directory, so interrupted iterations are preserved deterministically across crashes/resume.

2. Specify the required artifact/history behavior for interrupted and pre-launch-failure iterations. The spec currently says every iteration directory contains `stdout.txt`, `stderr.txt`, `final.txt`, and `meta.json`, allows `meta.json.status = interrupted`, and says interrupted iterations remain on resume, but it never says exactly what must be written when the provider/shell is stopped mid-run or cannot start cleanly. Add a single contract covering:
   - whether `meta.json` and `iteration_finished` are required for interrupted iterations,
   - what `status`, `exit_code`, `route`, and `transition_target` must be in those cases,
   - whether empty `stdout.txt`/`stderr.txt`/`final.txt` files are required when launch fails before useful output exists.

3. Resolve the Codex wrapper modal mismatch. Section 16.1 says Reflow only SHOULD use `--output-last-message`, while Section 16.3 says Codex `final.txt` comes from `--output-last-message` and Section 9.3 reserves that flag as wrapper-critical. That leaves two plausible implementations: one that always uses the flag, and one that treats it as optional and derives `final.txt` some other way. Make this consistently MUST, or define the exact fallback behavior if Codex final-message capture is allowed without that flag.

4. Make the provider failure classification boundary deterministic. Section 16.4 / 18.1 currently use phrases like "before useful work begins" and "after startup", which leave auth errors, invalid model errors, repo-check failures, and similar immediate CLI rejections open to inconsistent mapping between `provider_unavailable` and `step_failed`. Add an observable rule or explicit classification list that lets two implementations return the same `failure_type` and exit code for the same provider failure.

5. Remove or explicitly mark as informative the non-contract implementation prescriptions and duplicate summaries. Sections 24 and 26 prescribe an internal Python module layout and implementation phase order, which are code-structure choices rather than externally observable contracts. Sections 27 and 28 restate earlier scope/architecture points without adding new normative behavior. Because Section 1 says everything is normative unless marked otherwise, these sections currently fail the abstraction/economy criteria. Either delete them, or label them informative guidance and replace repeated material with cross-references.
2026-03-13 Writer: Revised `SAD.md` to make iteration reservation crash-safe and deterministic: Section 10.4 now defines the pre-launch persistence boundary, and Sections 11.1-11.3 now canonically define placeholder `meta.json`, empty-file requirements, `iteration_finished` obligations, interrupted reconciliation on `resume`/`stop`, and null-vs-integer `exit_code` behavior. Made Codex `--output-last-message` mandatory, narrowed `provider_unavailable` to OS-level exec failures while classifying all CLI-side rejections as `step_failed`, and marked Sections 24 and 26 informative while removing the duplicated summary sections. I strengthened Sections 10 and 11 as the single source of truth instead of restating the same artifact/recovery rules inline in every algorithm branch.
2026-03-13 Verifier: `SAD.md` is close, but one implementation-blocking lock/resume gap remains.

1. Make the workspace-lock behavior deterministic when `resume` encounters a non-stale `active.json` for a different live run in the same workspace. Section 6 says there is one active run per workspace and defines `active.json` as the workspace lock, but Section 20.2 only says `resume` refuses when another live controller owns the same run and then proceeds to write a fresh `active.json`. Two plausible implementations follow from the current text: one refuses to resume until the other run becomes terminal or stale, and another overwrites `active.json` and starts a second live controller in the same workspace. Add one canonical rule that covers both `run` and `resume`:
   - if `active.json` is stale, repair/replace it as already specified;
   - if `active.json` is non-stale and belongs to any other live run, `resume` MUST fail without modifying either run’s state or lock files;
   - if the same run already has a live controller, keep the existing refusal rule and state-preservation behavior explicit.

2. Tie the refusal path to an exact CLI result. Section 23 defines `25` for preflight/command-state errors before a new run starts, but it does not explicitly map `resume` command-state errors such as “run is missing”, “run is not running”, or “workspace lock belongs to another live run”. Add one rule stating the exit code and whether `run.json`, `history.jsonl`, and `active.json` remain unchanged on those refusal paths.
2026-03-13 Writer: Closed the remaining lock/resume gap in `SAD.md` by making Section 6.1 the canonical workspace-lock contract for both `run` and `resume`: stale locks are repairable, but any non-stale live lock now causes a command-state refusal with no changes to `active.json`, any `run.json`, or any `history.jsonl`, including the “same run already has a live controller” case. I aligned the runtime algorithm and CLI semantics to that rule and mapped all pre-ownership `resume` refusals (`run_id` missing, unreadable/non-running `run.json`, or live lock conflicts) to exit code `25` in Section 23 rather than duplicating bespoke outcomes inline.
