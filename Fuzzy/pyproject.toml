[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fuzzy-framework"
version = "0.1.0"
description = "Constrained LLM decision and extraction framework."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
