# Plan ↔ Plan Verifier Feedback

- 2026-03-15: Replaced the empty plan with an implementation-ready Priority 1 plan covering a shared `loop_control.py` module, canonical `<loop-control>` JSON schema, explicit legacy adapter rules, orchestrator integration points, fixture-backed parser tests, and a risk register so the implementation pass can centralize parsing without breaking current tag-based workflows.
- 2026-03-15: Refined the Priority 1 plan against the actual codebase: it now calls out legacy question-first semantics that must be preserved, the checked-in `.superloop` prompt files that also need updating because they are the active repo prompts, the exact shared parser API and failure mode, and the missing parser-fixture coverage needed to prevent a partial migration.
- PLAN-001 `blocking`: The canonical contract is still ambiguous when stdout contains more than one `<loop-control>...</loop-control>` block. Affected sections: `Canonical Contract -> Schema Rules`, `Shared Module And Interface -> Parser Behavior`. The plan says one payload represents exactly one decision, but the parser rule says to locate the last canonical block and decode it. In a concrete failure case, an agent could emit an earlier canonical question block and a later canonical promise block (or two conflicting promise blocks); the proposed parser would silently ignore the earlier control signal and change loop behavior without surfacing the protocol violation. Minimal correction: specify that multiple canonical blocks are invalid and must raise `LoopControlParseError` (or otherwise define one unambiguous acceptance rule and test it explicitly).
- PLAN-002 `blocking`: The test plan does not cover the orchestrator behaviors the plan claims must remain unchanged after the shared-parser refactor. Affected sections: `Call-Site Integration`, `Test Plan`, `Milestones 2 and 4`. The current matrix only exercises `parse_loop_control()` fixtures plus the existing git-tracking tests, so a refactor that accidentally changes `docloop.py`'s writer-promise fatal path, verifier missing-promise default, or `superloop.py`'s producer-promise ignore / COMPLETE-downgrade logic could still ship green. Minimal correction: add targeted tests for the preserved call-site semantics in both orchestrators, or explicitly add a small refactor that isolates those branch decisions into testable helpers and cover them there.
- PLAN-003 `non-blocking`: `prompt_guideline.md` still documents the current `<question>` plus final-line `<promise>` protocol as the stable Superloop prompt contract. If the implementation updates prompts and runtime to prefer canonical `<loop-control>` output, that guidance will become stale and may reintroduce prompt drift later. Updating it is not required for this migration to function, but it would keep the repo's prompt-design documentation aligned with the new contract.
- 2026-03-15: Tightened the plan to reject multiple canonical `<loop-control>` blocks as malformed protocol output, added explicit orchestrator-semantics regression coverage for the current question-first/defaulting behavior in `docloop.py` and `superloop.py`, and pulled `prompt_guideline.md` into the documentation scope so the runtime contract and prompt guidance stay aligned.
- 2026-03-15: Verified the revised loop-control migration plan against `docloop.py`, `superloop.py`, `README.md`, `prompt_guideline.md`, and the current test surface. The earlier multiple-canonical ambiguity, orchestrator-semantics coverage gap, and prompt-guideline drift concerns are addressed; no new blocking or non-blocking findings were identified in this pass.
