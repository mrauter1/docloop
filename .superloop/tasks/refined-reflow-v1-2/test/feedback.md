# Test Author ↔ Test Auditor Feedback

* 2026-03-16: Auditor review complete. `TST-001` is now covered in `tests/test_refined_reflow_sad.py:154-165`, the changed v1.2 SAD deltas are exercised directly, and no additional blocking or non-blocking findings were identified.
* 2026-03-16: Resolved `TST-001` by adding explicit regression coverage for the `reply` workspace-lock reacquisition rule in `tests/test_refined_reflow_sad.py`; updated the behavior-to-test map accordingly and re-ran `pytest -q tests/test_refined_reflow_sad.py` (`8 passed`).
* 2026-03-16: Expanded `tests/test_refined_reflow_sad.py` from coarse phrase checks into explicit coverage for canonical-source handling, Section 14 heading repair, `pending_input.auto_round`, full-auto success/budget/failure semantics, repository-resident memory, and `reply_started`/`input_auto_failed` history rules.
* 2026-03-16: `TST-001` (`blocking`) `tests/test_refined_reflow_sad.py:154-164` validates `reply_started` ordering, but it never asserts the new `reply` lock-safety requirement added in `refined_reflow_v1.2/SAD.md:1110-1112`: `reply` must reacquire the workspace lock before resolving input. If that sentence is removed or weakened, the suite still passes while the revised reply contract regresses on single-controller safety. Add an assertion in the reply-semantics test that pins the workspace-lock reacquisition rule alongside the existing `reply_started` ordering checks.
