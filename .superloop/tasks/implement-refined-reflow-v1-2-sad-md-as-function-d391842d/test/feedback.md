# Test Author ↔ Test Auditor Feedback

- Added regression coverage for the policy/signal/task-id fixes: unchanged escape symlinks stay allowed, `.reflow` tampering still fails, reserved iteration artifacts stay exempt, `SIGTERM` reconciles to `stopped`, long intent-derived task IDs stay bounded and createable, and long explicit task IDs still resume. Updated `test_strategy.md` with an explicit behavior-to-test map plus flake controls.
- `TST-001` (`blocking`): The reserved-iteration allowlist change is only proven on the agent path. [tests/test_reflow_runtime.py:759](/home/marcelo/code/docloop/tests/test_reflow_runtime.py#L759) covers `reflow_runtime.controller._execute_agent_step`, but the matching shell-path change at [reflow_runtime/controller.py:345](/home/marcelo/code/docloop/reflow_runtime/controller.py#L345) has only a negative tampering test at [tests/test_reflow_runtime.py:719](/home/marcelo/code/docloop/tests/test_reflow_runtime.py#L719). A regression that drops `ignored_paths` from `_execute_shell_step` would cause restrictive `allow_write` shell steps to fail on their own `.reflow/runs/...` transport files, and the current suite would still pass. Minimal correction: add a shell-step success test with restrictive `allow_write` that would fail unless the active iteration directory remains exempt while out-of-band `.reflow` writes stay blocked.
- Added `test_shell_policy_ignores_reserved_iteration_transport_artifacts` to close the shell-path gap: a restrictive `allow_write` shell step now proves `.reflow/runs/.../stdout.txt`, `stderr.txt`, `final.txt`, and `meta.json` stay exempt while the allowed workspace write still succeeds. Updated `test_strategy.md` so the reserved-iteration exemption is explicitly mapped for both agent and shell execution paths.

## 2026-03-16 Audit Pass

No new blocking or non-blocking findings. Verified the changed behaviors are covered across unit and integration-style tests: unchanged escape symlinks, `.reflow` tamper detection, reserved-iteration exemptions on both agent and shell paths, stop reconciliation on `SIGTERM`, and bounded-vs-explicit Superloop task ID behavior. Confirmed with `pytest -q tests/test_reflow_runtime.py tests/test_superloop_observability.py` (`48 passed`).
