# Plan ↔ Plan Verifier Feedback

## 2026-03-16

- Replaced the placeholder plan with an implementation-ready Reflow v1.2 runtime plan grounded in the current repo state: no existing runtime, a docs-only `reflow/` directory that must stay non-package, and no dependency manifest yet.
- Defined the proposed `reflow.py` plus `reflow_runtime/` architecture, command and persistence interfaces, milestone acceptance criteria, test strategy, implementation checklist, and risk controls so the implementation pair can execute against one concrete source of truth.
- `PLAN-001` (`blocking`): The plan does not pin the required `status` / `list` output contract. Affected sections: "Locked implementation assumptions", "Core runtime interfaces", and Milestone 1. The plan says only that `status` and `list` will be "stable human-readable text output" and that Milestone 1 should "Implement read-only `status` and `list`", but it never carries forward the SAD Section 17.5 minimum fields or `list` sort rule. Concrete failure scenario: implementation ships a terse `status` output that omits pending-input `auto_round` or a `list` command that is not sorted by `started_at` descending; the CLI would appear complete against this plan but still violate the normative contract and fail late in review/tests. Minimal correction: add the exact required `status` fields, the conditional failed/awaiting-input fields, the `list` field minimums, and the `started_at` descending sort requirement to the interface/acceptance/test sections.
- `PLAN-002` (`blocking`): The shell-step runtime contract is still too loose for implementation. Affected sections: "Core runtime interfaces", Milestone 2 deliverables, and the implementation checklist. The plan mentions `/bin/sh -lc` and generic "environment-variable handling", but it does not name the mandatory `REFLOW_RUN_ID`, `REFLOW_WORKFLOW`, `REFLOW_STEP`, `REFLOW_LOOP`, `REFLOW_WORKSPACE`, and `REFLOW_ITERATION_DIR` variables from SAD Section 19, nor the exact `command.txt` / `meta.json.command_argv` / `meta.json.command_text` obligations from Sections 12.2-12.3. Concrete failure scenario: shell workflows or fixture scripts depend on `REFLOW_ITERATION_DIR` or `REFLOW_STEP`, while the implementation only sets a partial environment and still passes the current milestone wording; debugging and conformance then fail after significant coding churn. Minimal correction: make the exact shell env/artifact contract explicit in the plan and add dedicated acceptance/tests that assert the environment variables and recorded shell command artifacts.
- `PLAN-003` (`non-blocking`): The plan would be easier to execute consistently if the provider execution contract also called out `timeout_sec` and merged provider `env` behavior from SAD Section 10.4 in the same place as argv construction. The current plan likely still leads implementers back to the SAD, but centralizing those execution-environment rules would reduce lookup churn and lower the chance of partial wrapper implementations.

- Refined `plan.md` to close the verifier gaps: added the exact `status`/`list` minimum output contract and `started_at` sort rule, the provider `timeout_sec` plus merged `env` execution rules, and the full shell-step `REFLOW_*` environment and command-artifact contract.
- Propagated those requirements into Milestone 1 and 2 acceptance criteria, the implementation checklist, unit/integration coverage, and the risk register so the implementation pair can validate them without rediscovering SAD details mid-build.
- Verification follow-up: no new findings. Prior `PLAN-001` through `PLAN-003` are resolved in the current plan, and the plan now satisfies correctness, completeness, regression-risk, DRY/KISS, and feasibility checks for this task scope.

- Replaced the stale greenfield plan with a delta-focused plan grounded in the current repository state: the Reflow runtime, CLI, dependency file, README guidance, and focused tests already exist and pass.
- Reframed milestones around remaining work only: command-state and loader hardening, provider/protocol conformance coverage, and end-to-end regression closure, so the implementation pair does not waste cycles re-planning completed scaffolding.
- Tightened the plan’s interfaces and risk controls around `active.json`, YAML/dependency behavior, provider wrapper verification, and full-auto iteration invariants because those are the most likely remaining verifier and regression hotspots.
- `PLAN-004` (`blocking`): The plan’s narrowed “remaining work” omits the repository’s still-open policy-enforcement blockers. Affected sections: "Objective", "Scope", "Code areas to touch", Milestones 1-3, "Test strategy", and the risk register. Evidence in-repo: `.superloop/tasks/implement-refined-reflow-v1-2-sad-md-as-function-d391842d/implement/criteria.md` still has `Correctness` and `Safety` unchecked, and `.superloop/tasks/implement-refined-reflow-v1-2-sad-md-as-function-d391842d/implement/feedback.md` records unresolved `IMP-008` and `IMP-009` in `reflow_runtime/policy.py` covering unchanged external-symlink handling and invisible `.reflow/` mutations. Concrete failure scenario: the implementation pair follows this plan, hardens only loader/controller/provider/protocol paths, and exits with the known `policy.py` blockers still live, so the task cannot converge to COMPLETE despite the plan claiming the remaining work is elsewhere. Minimal correction: bring `reflow_runtime/policy.py` and its regression tests back into scope explicitly, add a milestone or acceptance criteria for the `IMP-008`/`IMP-009` fixes, and extend the risk register/tests to cover policy enforcement against unchanged escape symlinks and child writes under `.reflow/`.
- 2026-03-16: Replaced the stale plan with a delta-focused implementation plan tied to the actual unresolved blockers in `implement/feedback.md`. The new plan explicitly covers `IMP-001` through `IMP-009`, restores `reflow_runtime/policy.py` and its regression tests to scope, and defines concrete milestones for stop/interrupt semantics, pending-input invariants, malformed-control retries, child PID ownership, shell/provider contract conformance, and `.reflow` policy enforcement so the implementation pair can execute without re-deriving requirements from the SAD.
- 2026-03-16: Re-verified the current `plan.md` against the task prompt, product context, `implement/feedback.md`, and spot-checked SAD contracts for `status`/`list`, `pending_input`, `active.json`, shell `REFLOW_*` environment, and provider timeout/env behavior. No new findings: the plan is consistent with the known blockers, keeps the remaining work delta-focused, and is implementation-ready for correctness, completeness, regression-risk, DRY/KISS, and feasibility criteria.
