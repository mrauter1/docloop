# Test Author ↔ Test Auditor Feedback

- 2026-03-20: No repo runtime tests were added for phase 1 because the scoped changes are dependency/docs only and existing test modifications are out of scope. Added a behavior-to-coverage map in `test_strategy.md` that ties AC-P1-1/2 to static artifact inspection and AC-P1-3 to the required fresh-install full-suite runs from both `requirements.lock` and bounded `requirements.txt`.
- 2026-03-20: No blocking or non-blocking audit findings in the phase-1 test artifacts. The strategy maps the dependency-baseline acceptance criteria to the correct validation layers, includes explicit edge-case and failure-path regression coverage, and documents deterministic fresh-environment execution to control flake risk.
- 2026-03-20: Added the phase-2 coverage map in `test_strategy.md` for auth browser-submit CSRF, deferred requeue race hardening, crossed-completion idempotence, the six requested reaper tests, the manual reaper script contract, and the pinned-environment full-suite validation. The runtime additions are `triage-stage1/tests/test_phase2_auth_worker_hardening.py` and `triage-stage1/tests/test_stuck_run_reaper.py`, which passed as focused modules (`12 passed`) before two passing full-suite runs (`65 passed` each).
- 2026-03-20: No blocking or non-blocking audit findings in the phase-2 test artifacts. The strategy now maps AC-P2-1 through AC-P2-4 to the actual new runtime tests and pinned-environment validation, and the chosen techniques keep the race/reaper coverage deterministic by using explicit timestamps, local SQLite fixtures, `TestClient` browser-style form flow, and controlled monkeypatched error paths.
- 2026-03-20: Refined the phase-2 reaper coverage after the concurrency hardening review by adding the explicit single-claimer threaded regression and documenting its event-gated stabilization in `test_strategy.md`. The focused phase-2 modules now account for `13 passed`, and the final pinned-environment Stage 1 suite result is `66 passed`.
- 2026-03-20: No blocking or non-blocking audit findings in the final phase-2 test diff. The strategy covers the auth/browser CSRF path, requeue race handling, crossed completion, the requested reaper behaviors, and the added single-claimer concurrency guard, while the updated stabilization notes and final runtime results (`7 passed` for the reaper module, `66 passed` for the full Stage 1 suite) keep the audit trail consistent with the tested final state.
